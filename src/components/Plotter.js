import * as Danfo from 'danfojs/dist/index';
import { useState, useEffect } from 'react';

const Plotter = ({ datasetUrl, columns, plot, plotType,}) => {
    return (
        <div className="current-plot-container">

        </div>
    )
};

export default Plotter;